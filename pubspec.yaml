name: dart_ewelink_api
description: Pure dart pacakge to access and interact with eWeLink API and services
version: 0.0.6
homepage: https://github.com/ianmaciel/dart_ewelink_api

environment:
  sdk: ">=2.18.2 <4.0.0"

dependencies:
  crypto: ^3.0.3
  http: ^1.1.0
  json_annotation: ^4.8.1
  nonce: ^1.2.0

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  test: ^1.24.6
